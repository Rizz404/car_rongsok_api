# Environment Configuration Template
# Copy this file to .env and update with your actual values
# .env is gitignored and contains your local secrets

# Node Environment
NODE_ENV=development

# Database Configuration
# Local: postgres://postgres:password@localhost:5432/car_db
# Railway: Copy from Railway PostgreSQL service
DATABASE_URL=postgres://postgres:password@localhost:5432/car_db

# JWT Secret
# Generate a secure random string: openssl rand -hex 32
# Local development: any string
# Production: use a secure random string
JWT_SECRET=dev-secret-change-in-production

# Server Port (optional, defaults to 3000)
# Railway will set this automatically
PORT=3000
